<%= form_for(@service_attachment) do |f| %>
  <% if @service_attachment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@service_attachment.errors.count, "error") %> prohibited this service_attachment from being saved:</h2>

      <ul>
      <% @service_attachment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :service_id %><br>
    <%= f.number_field :service_id %>
  </div>
  <div class="field">
    <%= f.label :picture %><br>
    <%= f.text_field :picture %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
